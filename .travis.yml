language: node_js

node_js:
  - node

script: eslint src --ignore-pattern "src/**/*\.test\.js" && npm test

cache:
  yarn: true
  directories:
    - node_modules

branches:
  only:
    - master

# after_script:
  # - cat coverage/lcov.info | node_modules/.bin/coveralls
